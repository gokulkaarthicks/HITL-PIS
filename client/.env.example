# Copy to client/.env
# Base URL of the FastAPI backend. No secrets belong in this file: anything
# prefixed VITE_ is inlined into the JavaScript bundle and is public.
VITE_API_BASE_URL=http://localhost:8000
